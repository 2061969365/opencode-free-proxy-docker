# opencode-free-proxy 免费额度限流（429）说明

本文说明 Claude Code 经本代理使用 opencode zen 免费模型时，可能遇到的"免费额度限流"（HTTP 429 rate_limit_error）现象：原因、代理已做的处理、仍然会发生的场景，以及解决办法。

---

## 1. 现象

在 Claude Code 会话中表现为：

- 突然中断 / 报错：`rate_limit_error`（HTTP 429）
- 密集使用时（多个子代理并行、快速连续多轮工具调用）更容易触发
- 错误通常出现在一次会话中的某个请求上，重试后往往能恢复

## 2. 根因（已查明）

opencode zen 免费 API 按 **User-Agent 白名单** 限流，不是按账号/Key：

| 请求特征 | 结果 |
|---|---|
| `User-Agent: opencode/1.18.18`，**不带** `Authorization` 头 | ✅ 放行 |
| 带 `Authorization: Bearer xxx`（哪怕是 `Bearer public` 或空 Bearer） | ❌ 429 |
| UA 为 `undici` / `curl` / 无 UA | ❌ 429 |

opencode 官方桌面版之所以能用，正是因为它的请求不带 `Authorization`、UA 是 opencode。

## 3. 代理已做的处理

1. **固定 UA**：所有上游请求固定发送 `User-Agent: opencode/1.18.18`（`src/constants.js`）
2. **不转发鉴权头**：客户端的 `Authorization` 一律丢弃，不上送 zen
3. **禁止客户端覆盖**：`req.headers` 全程不被读取，客户端无法注入自定义 UA / Authorization 破坏白名单
4. **自动重试**：遇到 429 / 5xx（500/502/503/504）自动退避重试，间隔 2s → 4s → 8s，最多 3 次（`src/proxy.js` 的 `fetchUpstream`）
   - 瞬时限流（一两秒后恢复）会被自动扛过去，不再立刻报错中断

## 4. 仍然会限流的场景（重试救不了）

免费层存在**硬性频率上限**（如每分钟 / 每窗口允许的请求数）。以下情况会突破硬上限，重试也无效：

- **并发子代理**：Claude Code 一次派出多个并行子代理，短时间叠加大量请求
- **高频多轮工具调用**：一个回合内连续快速调用工具，多个请求挤在同一时间窗
- **长时间连续使用**：免费窗口被持续打满

判断方法：打开 `http://127.0.0.1:4096/api/logs`，查看 `status=429` 且短时间内连续多条的记录。

## 5. 解决办法

| 方案 | 做法 |
|---|---|
| **放缓节奏**（最直接） | 减少并行子代理数量；工具任务之间留间隔，避免一口气连发 |
| **等待恢复** | 429 通常是窗口制，等 30–60 秒窗口滚动后即可继续 |
| **换非免费模型** | 在 `config/mappings.json` 或代理页面把映射改到非 `-free` 型号（如 `deepseek-v4-flash`），免费限流通常不适用于付费型号 |
| **确认不是别的问题** | 先看 `/api/logs`：只有 `status=429` 才属于限流；`status=500/超时` 属于其他问题（如上游慢、代理错误） |

## 6. 与"说着说着中断"的关系

之前 Claude Code "说着说着中断"的主要根因是**协议层**（工具调用块被丢弃导致卡住、usage 崩溃等），这些已修复。

限流是**唯一无法彻底消除**的剩余因素——代理已用退避重试把"瞬时限流"扛过去，但**密集调用触发的硬限流**只能靠放缓节奏规避，无法从代理层面根除。

---

*文档基于本代理实测（opencode.ai/zen，模型 deepseek-v4-flash-free）与多轮端到端验证整理。*
